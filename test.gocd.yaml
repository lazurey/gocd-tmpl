pipelines:
  build:
    group: frontend
    label_template: "${COUNT}"
    materials:
      source_code:
        git: "git@git.thoughtworks.net:bookstore/bookstore-frontend.git"
        branch: master
    stages:
      - build:
          jobs:
            pre_build:
              tasks:
                - script: ./pipeline-scripts/pre_build.sh
